# serializer version: 1
# name: test_redundant_parentheses
  '''
  result = foo.bar.cat()
  value = foo.bar.cat()
  another = obj.method()
  attr_only = foo.bar.cat
  also_remove_these = foo.bar.cat(arg1, arg2)
  keep = (foo + bar)
  keep2 = ('Traceback(filename={!r}, '
              'lineno={!r},)'.format(
              self.filename, self.lineno))
  '''
# ---
