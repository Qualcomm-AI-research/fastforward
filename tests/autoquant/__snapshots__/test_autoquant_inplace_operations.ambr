# serializer version: 1
# name: test_autoquant_quantizes_inplace_operations
  '''
  def forward(self, x: torch.Tensor, y: torch.Tensor) -> torch.Tensor:
      x = self.quantizer_x(x)
      y = self.quantizer_y(y)
      x = fastforward.nn.functional.add(x, y, output_quantizer=self.quantizer_add)
      x = fastforward.nn.functional.mul(x, y, output_quantizer=self.quantizer_mul)
      x = fastforward.nn.functional.sub(x, y, output_quantizer=self.quantizer_sub)
      return x
  
  '''
# ---
