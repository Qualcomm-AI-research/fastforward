{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "process",
			"label": "build",
			"command": "./scripts/rebuild",
			"args": [
				"${workspaceFolder}",
				"${env:CONDA_DEFAULT_ENV}"
			],
			"runOptions": {
				"runOn": "folderOpen"
			},
			"group": "build",
			"problemMatcher": [],
			"detail": "Install in editable mode"
		}
	]
}